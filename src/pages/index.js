import * as React from 'react';
import Layout from '../components/Layout/layout';

// markup
const IndexPage = () => {
	return (
		<Layout>
			<h1>Portfolio</h1>
		</Layout>
	);
};

export default IndexPage;
